# add under your existing top-level config
jobs:
  - id: bit_tt2l
    type: bit
    process: tt2l                 # loader/view name from data.samples
    selection: null               # optional extra selection layered on top
    pdf:
      cheb_n: 5                   # <-- controls the number of Chebyshev coeffs (c0..cN)
    model:
      n_trees: 200
      learning_rate: 0.2
      loss: MSE                   # or "CrossEntropy"
      learn_global_score: true
    runtime:
      training_plots: true        # keep if you want training plots
    output:
      filename: "BIT_tt2l.pkl"

  - id: bit_tt2l_GG
    type: bit
    process: tt2l_GG                 # loader/view name from data.samples
    selection: null               # optional extra selection layered on top
    pdf:
      cheb_n: 5                   # <-- controls the number of Chebyshev coeffs (c0..cN)
    model:
      n_trees: 200
      learning_rate: 0.2
      loss: MSE                   # or "CrossEntropy"
      learn_global_score: true
    runtime:
      training_plots: true        # keep if you want training plots
    output:
      filename: "BIT_tt2l_GG.pkl"

  - id: bit_tt2l_QG
    type: bit
    process: tt2l_QG                 # loader/view name from data.samples
    selection: null               # optional extra selection layered on top
    pdf:
      cheb_n: 5                   # <-- controls the number of Chebyshev coeffs (c0..cN)
    model:
      n_trees: 200
      learning_rate: 0.2
      loss: MSE                   # or "CrossEntropy"
      learn_global_score: true
    runtime:
      training_plots: true        # keep if you want training plots
    output:
      filename: "BIT_tt2l_QG.pkl"

  - id: bit_tt2l_QQ
    type: bit
    process: tt2l_QQ                 # loader/view name from data.samples
    selection: null               # optional extra selection layered on top
    pdf:
      cheb_n: 5                   # <-- controls the number of Chebyshev coeffs (c0..cN)
    model:
      n_trees: 200
      learning_rate: 0.2
      loss: MSE                   # or "CrossEntropy"
      learn_global_score: true
    runtime:
      training_plots: true        # keep if you want training plots
    output:
      filename: "BIT_tt2l_QQ.pkl"
