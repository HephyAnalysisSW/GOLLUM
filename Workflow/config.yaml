Tasks:
#  - Classifier
  - IC
#  - ICP
#  - BPT
#  - Uncert_JES
#  - Fit

Classifier:
  module: "Model"
  model: "/path/to/model" # path of the trained ML model 
  input: "/path/to/input"
  output: "/path/to/output"
  hyper_param:
    nepochs: 100
    nlayers: 3

IC:
  module: "IC"
  model_name_rel: "IC_1203" # reletive path of the trained ML model 
  model_name_abs: "" # absolute path of the trained ML model 
  input: "/path/to/input"
  output: "/path/to/output"
  selection: "lowMT_VBFJet"

ICP:
  module: "ICP"
  model_name_rel: "ICP_1203" # reletive path of the trained ML model 
  model_name_abs: "/groups/hephy/cms/ang.li/HiggsChallenge/models/ICP/ICP_1203_2" # absolute path of the trained ML model 
  input: "/path/to/input"
  output: "/path/to/output"
  config: "ML.configs.icp_quad_jes"
  selection: "lowMT_VBFJet"
  hyper_param:
    #nepochs: 100
    #nlayers: 3

BPT:
  module: "BoostedParametricTree"
  model_name_rel: "model_name" # reletive path of the trained ML model 
  model_name_abs: "" # absolute path of the trained ML model 
  input: "/path/to/input"
  output: "/path/to/output"
  hyper_param:
    nepochs: 100
    nlayers: 3


Uncert_TES:
  module: "TES"
  model_name_rel: "model_name" # reletive path of the trained ML model 
  model_name_abs: "" # absolute path of the trained ML model 
  input: "/path/to/input"
  output: "/path/to/output"
  hyper_param:
    nepochs: 100
    nlayers: 3

Fit:
  # Not clear yet what to put for this...
